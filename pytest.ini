[pytest]
addopts = -p no:cacheprovider
pythonpath = .
testpaths = tests
norecursedirs = .git .hg .svn .tox .nox .mypy_cache __pycache__ .hypothesis build dist _archive venv .venv
markers =
    integration: marks tests as integration tests requiring a live system
